const express = require("express");

const router = express.Router();

// router.post("/register", buyerRegister);
// router.get("/buyers", buyers);
// router.post("/signin", buyerSignin);
// router.patch("/update:id", buyerUpdate);
// router.post("/signout", buyerSignOut);
// router.delete("/delete:id", buyerDelete);

module.exports = router;
